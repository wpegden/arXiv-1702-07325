# Paper Notes

## Paper-Check Status
- Checked the full manuscript on 2026-03-25.
- No genuine contradiction or irreparable proof gap was found.
- The main issues are proof clarifications that should be made explicit before Lean formalization.

## Formalization-Critical Modeling Choices
- The phrase "roommates do not care about one-cent error margins" needs a precise mathematical replacement. The proofs use a local-constancy principle: after choosing a triangulation with sufficiently small mesh, the acceptable-room data is stable on each small simplex. For Lean it will be cleaner to formalize the paper directly in terms of Sperner-style labelings on a triangulation, with the rent/preference discussion recorded as motivation.
- At a rent division lying on a proper face of the simplex, the free rooms are exactly the rooms whose coordinates are zero, i.e. the vertices not belonging to that face. This complement convention is easy to lose track of and is responsible for the permutation trick in the main proof.

## Corrections And Clarifications
- Section 2, Sperner lemma, Proof 1: the trap-door argument is standard and correct, but the parity claim about inaccessible fully labeled simplices is compressed. The clean formulation is that every connected component of the door graph without a boundary door is a path or cycle, so inaccessible degree-1 rooms occur in pairs.
- Section 2, Sperner lemma, Proof 2: the surjectivity step is deferred to Section 5. For the formalization plan, treat the needed lemma as: a piecewise-linear self-map of a simplex that preserves every face setwise hits the barycenter.
- Section 3, three-roommates, Proof 1: the nine pair-labels are partitioned into three cyclic classes. A fully labeled small triangle forces Larry and Moe each to use at least two room labels on that triangle, and also forces every room label to appear for at least one of them. This is the exact combinatorial statement needed later; it is worth isolating as a finite case check rather than leaving it as "simple to check."
- Section 3, three-roommates, Proof 2: the boundary-degree-one claim is valid once the original vertices are chosen so that both visible roommates pick the same free room at each vertex, and the three rooms are chosen once each around the boundary. Then each boundary edge maps to the corresponding adjacent edge path, so the image winds once around the triangle.
- Section 4, general case, lines 283-301: the permutation/Sperner argument is mathematically sound but too terse. For a boundary vertex supported on a proper face with vertex set `I`, choose `i in I` with `pi(i) notin I`; such an `i` exists because `pi` is an `n`-cycle, so no proper nonempty subset is `pi`-stable. Room `pi(i)` is free on that face, so we may assign that room. After composing with `f^-1`, the encoded label is `i`, which lies in the supporting face. This is the real reason `f^-1` applied to `lambda_j` gives a Sperner labeling.
- Section 4, Hall step: from the proved statement "any `k` visible roommates jointly see at least `k+1` room labels on `tau`", deleting the secretive roommate's chosen room leaves at least `k` available labels for every `k`-subset. That is the exact Hall condition for the remaining bipartite graph.
- Section 5 / support API correction: the paper's "piecewise linear on each simplex" hypothesis must mean agreement with one affine chart on the whole simplex realization, not merely on its vertices. The earlier Lean wrapper for `IsPiecewiseAffineOn` only matched vertex values, which was too weak to convert `FacetImageContains` statements into actual point witnesses inside one cell. The corrected definition now agrees with the manuscript and is what the Section 5 local geometry really uses.
- Section 5, algorithmic section: the argument is conceptually correct but written at a sketch level. The genericity assumption should be read as a perturbation of the barycenter chain so that the preimage of the chain under `lambda` is a finite 1-dimensional cell complex. Then the component containing `e1` has one endpoint at `e1`; its other endpoint must be an `(n-1)`-simplex whose image contains the barycenter. This is the repair to keep in mind if the text needs to be formalized more rigorously.
- Section 5, start of the path-following argument: besides the perturbation-to-a-1-complex claim, the sentence about "the edge of `T` that contains `e_1` and subdivides `[e_1,e_2]`" implicitly uses that the triangulation restricts to an honest 1-dimensional simplicial subdivision of the boundary face `[e_1,e_2]`. In Lean this is a separate structural requirement: the current finite triangulation wrapper does not by itself expose the induced boundary subdivision or the resulting uniqueness of the first edge at `e_1`.
- Section 5, higher-dimensional continuation step: the paper's sentence "a line segment generically cannot intersect a `k`-face in more than two of its `(k-1)`-faces and it intersects in precisely one `(k-1)`-face if it ends inside the `k`-face" is doing the real work behind upper-continuation uniqueness and the endpoint rule. The current Lean support layer already recovers lower-continuation uniqueness from affine independence, but the remaining two fields of the local 1-complex picture do not follow from face preservation and convex-hull intersection data alone; they require this additional generic segment-intersection input, or an equivalent explicit perturbation hypothesis.
- Section 5, current local witness status: after correcting `IsPiecewiseAffineOn`, the support layer now proves both directions of the realization/hull bridge for one cell. `FacetImageContains f σ y` yields an actual point of `σ.realization` mapping to `y`, and conversely any point of `σ.realization` maps into `FacetImageHull f σ`. This is the missing API needed to attack the remaining perturbation fields by working with real points of one simplex cell instead of only convex-hull intersection data.
- Section 5, current lower-step status: the Lean support layer now also formalizes the support-shrinking half of the manuscript's predecessor construction. If one already has a point of a Section 5 cell in the lower prefix face whose image stays on `[b_k,b_{k+1}]`, the new endpoint lemma shows that the image is automatically `b_k`; then that point lies in the realization of the filtered lower subface `section5LowerPrefixVertices`, and the piecewise-affine chart forces that lower subface's image hull to contain `b_k`. In addition, the development now has the more direct manuscript-shaped entry theorem in the exact form the prose suggests: if the geometry supplies an actual lower face `τ ≤ u.cell` with `u.level` vertices in the lower prefix face and even just a single point of `τ.realization` whose image stays on `[b_k,b_{k+1}]`, convexity puts that point in the lower prefix face, the endpoint lemma upgrades the image to `b_k`, and the predecessor `Section5Step` follows immediately. The new finite-combinatorial route now makes this concrete: for each cell there is a finite family `section5SegmentSubfaces u f` of nonempty subfaces whose image meets `[b_k,b_{k+1}]`, a minimal-cardinality such face exists, the corrected piecewise-affine API extracts an actual point of that minimal face mapping to the segment, and the lemma `IsPiecewiseAffineOn.exists_point_with_nonzero_weights_of_minimal_section5SegmentSubface` upgrades this to a witness with nonzero barycentric weight on every vertex of the chosen face. The newest helper `IsPiecewiseAffineOn.exists_erased_face_point_of_minimal_section5SegmentSubface` now carries out the domain-side renormalization step explicitly: after choosing one vertex `v`, it constructs a point `x'` in the erased face realization and a coefficient `0 < c < 1` such that the original witness is `lineMap x' v c` and its image is `lineMap (f x') (f v) c`. So the remaining geometric burden is now completely explicit and codomain-side: show that when `v` lies outside the lower prefix face, this affine image formula still forces `f x'` to stay on `[b_k,b_{k+1}]`, so erasing `v` preserves segment-hitting and contradicts minimality. This matches the manuscript more closely than the older filtered-lower-prefix detour and isolates the remaining issue to one genuine geometric contradiction lemma on that canonical relative-interior point. That older `section5LowerPrefixVertices` route is still partially developed: any actual lower-face preimage of `b_k` has simplex support exactly `prefixRooms n k`, and any filtered lower subface whose image hull already contains `b_k` must use every room among the first `k` in the support of some vertex. The unresolved geometric work is therefore best understood as the genuinely manuscript-level part: prove that every vertex of the minimal segment-hitting face already lies in the lower prefix face, together with the resulting codimension-one cardinality statement, rather than trying to force the whole filtered lower-prefix collection to be that face.
- Section 5, codomain warning on the minimal-subface route: the tempting standalone lemma exposed by the erased-face package is false without extra genericity. Even inside `ambientCoordinateFace (prefixRooms 3 3)`, one has `(2/5,2/5,1/5) ∈ [b₂,b₃]` and
  `(2/5,2/5,1/5) = lineMap (1/5,3/5,1/5) (3/5,1/5,1/5) (1/2)`,
  but neither endpoint lies on `[b₂,b₃]`. So from
  `f x = lineMap (f x') (f v) c ∈ [b_k,b_{k+1}]` with `0 < c < 1` and only the ambient-face
  support constraints, one cannot conclude `f x' ∈ [b_k,b_{k+1}]`. The manuscript's
  perturbation/genericity sentence therefore has to be read as a stronger transversality claim on
  how the barycenter segment meets simplex images, not as something derivable from the current
  face-preserving + piecewise-affine API alone.
- Section 5, current formalization status: this remaining manuscript sentence is now represented directly by the support-layer structure `Section5BoundarySegmentGenericity`. That package is already sufficient, together with the canonical start-successor theorem, to derive the paper-facing Section 5 target-facet conclusion; the unresolved work is therefore the actual geometric proof of `Section5BoundarySegmentGenericity`, not any further graph-theoretic packaging.
- Section 5, current proof boundary: the Lean development now also has the more literal step-level perturbation package `Section5PerturbationGenericity`, whose fields say that a non-start cell is entered from a lower face, has at most one upper continuation as a `Section5Step`, and if there is no such continuation then it already contains the barycenter. This matches the manuscript's segment-intersection language more closely than the older neighbor-cardinality packaging, so the remaining work is best understood as proving `Section5PerturbationGenericity` from the paper's genericity/perturbation claim.
- Section 5, exact local gap: for the current proof route, the honest missing hypothesis is now isolated as `Section5MinimalSliceLowerBoundaryFaceData u f`. It asks for a minimal segment-hitting face together with one codimension-one lower face whose vertices lie in the lower prefix face and which contains a point mapping into `[b_k,b_{k+1}]`. The Lean support layer already proves that this local data upgrades immediately to `Section5MinimalSliceLowerBoundaryGeometry u f`, and then to the canonical lower-entry pair. So if the manuscript's quoted genericity sentence is taken as an axiom-like perturbation input, this is the precise local form it should have.
- Section 5, exact next theorem target: the development now names the still-unproved local sentence itself as `Section5MinimalSubfaceLowerBoundaryGenericity u f`. This is the theorem on a minimal `τ ∈ section5SegmentSubfaces u f` that the manuscript's quoted line should provide: `τ` has a codimension-one lower face in the lower prefix face that still meets `[b_k,b_{k+1}]`. Once this theorem is proved, Lean already upgrades it immediately to `Section5MinimalSliceLowerBoundaryFaceData u f` and hence to the current lower-boundary geometry package.
- Section 5, single precise remaining geometric sublemma: the current API still lacks the manuscript's actual local transversality statement on one minimal segment-hitting face `τ`. What is needed next is not another graph/package theorem, but the direct slice-endpoint claim that `section5CellSlice u f ∩ τ.realization` is a 1-dimensional segment whose lower endpoint lies in a unique codimension-one face `ρ ≤ τ`, with every vertex of `ρ` in `coordinateFace (prefixRooms n u.level)`. Any point of that endpoint face mapping into `[b_k,b_{k+1}]` then yields `Section5MinimalSubfaceLowerBoundaryGenericity u f` immediately.
- Section 5, optimizer recovery route: the alternative minimization strategy is now partially formalized via the linear functional `outsideMass I y = ∑_{i ∉ I} y_i`, with Lean lemmas showing nonnegativity, vanishing exactly on `coordinateFace I`, positivity off that face, and affine behavior along `lineMap`. This reduces the remaining gap on a minimal `τ` to one exact affine-fiber statement: if `x ∈ τ.realization` maps to `[b_k,b_{k+1}]` and minimizes `outsideMass (prefixRooms n k)` on that slice set, then any outside-prefix vertex carrying positive barycentric weight at `x` must admit another point `x'` in the erased face realization with the same image `f x' = f x`. That is the missing contradiction lemma needed to force the minimizer into the lower prefix face without first proving the full unique-endpoint theorem.
- Section 5, optimizer recovery route, checked support status: the slice/minimizer layer is now present in Lean, not just planned. `section5SubfaceSliceSet u τ g` packages the slice of one affine chart against `[b_k,b_{k+1}]`, `isCompact_section5SubfaceSliceSet` and `IsPiecewiseAffineOn.section5SubfaceSliceSet_nonempty` prove the slice is compact and nonempty on every segment-hitting face, and `IsPiecewiseAffineOn.exists_outsideMass_minimizer_on_section5SegmentSubface` chooses a genuine `outsideMass` minimizer on a minimal `τ`. So the optimizer route is now blocked on one precise codomain/geometric lemma only: turning a positive outside-prefix barycentric weight at that minimizer into an erased-face point with the same slice image, or at least another erased-face point still mapping into `[b_k,b_{k+1}]`.
- Section 5, optimizer recovery route, exact Lean interface: the downstream minimality API already accepts that weaker input. The new lemmas `minimal_section5SegmentSubface_vertices_mem_coordinateFace_of_erase_same_image` and `IsPiecewiseAffineOn.minimal_section5SegmentSubface_mem_coordinateFace_of_erase_same_chartImage` show that once one constructs an erased-face point with the same image under `f`, or even just under one affine chart on `τ`, the current development immediately forces the chosen minimal slice point into the lower prefix face. So the remaining obstruction is exactly the erased-face same-image construction itself, not any further translation from that construction into the existing lower-boundary pipeline.
- Section 5, optimizer recovery route, codomain normal form: the target segment `[b_k,b_{k+1}]` is now characterized in Lean by explicit coordinates, not just as an abstract segment. Namely, `y ∈ prefixBarycenterSegment n k` iff `y ∈ ambientCoordinateFace (prefixRooms n (k + 1))`, the level-`k` coordinate satisfies `y_k ≤ 1 / (k + 1)`, and every lower coordinate satisfies `y_i = 1 / k - y_k / k`. This means the remaining erased-face check can be attacked coordinatewise on the upper prefix face instead of by preserving the entire affine-chart image verbatim.
- Section 5, optimizer recovery route, support-pruning status: the first half of that coordinatewise codomain check is now proved. If a point of `[b_k,b_{k+1}]` is expressed as a nontrivial affine combination of two simplex points, then both endpoints already lie in `ambientCoordinateFace (prefixRooms n (k + 1))`. Applied to the existing erased-face line-map decomposition on a minimal segment-hitting face, this yields an erased-face point whose chart image is already known to lie in the upper prefix face. The remaining codomain work is therefore only the level-`k` bound and the lower-coordinate formulas, not upper-prefix support.
- Section 5, optimizer recovery route, hard model case: the special larger segment `[b_k, e_{k+1}]` is now fully characterized in Lean. `Section5Path.lean` proves not only that every point on `[b_k, e_{k+1}]` lies in `ambientCoordinateFace (prefixRooms n (k + 1))` and satisfies the lower-coordinate formulas `y_i = 1 / k - y_k / k` for `i < k`, but also the converse: those equations already force membership in `[b_k, e_{k+1}]`. Combined with the older scalar bound `y_k ≤ 1 / (k + 1)`, this upgrades the hard-case model point back into `[b_k,b_{k+1}]`. So the remaining erased-face codomain problem is now completely concrete: prove those formulas, plus the existing level-coordinate bound, for the erased-face chart image.
- Section 5, optimizer recovery route, sharp conditional reduction: the development now also proves a very specific fallback theorem. If, in the erased-face line-map decomposition on a minimal segment-hitting face, the erased vertex already has affine-chart image equal to the upper standard basis vector `e_{k+1}`, then the erased-face point is forced back into `prefixBarycenterSegment n k`, and the existing minimality pipeline closes immediately. This is a useful diagnostic reduction because it isolates one exact codomain identity whose truth would finish the proof; but it is stronger than the manuscript's prose and should not be mistaken for a consequence of face-preservation alone without further local geometric input.
- Section 5, optimizer recovery route, checked obstruction: the repo now also contains a Lean counterexample showing that the remaining hard-case lower-coordinate formulas do not follow from the currently available codomain data alone. In `n = 4`, `k = 2`, there are `y, w ∈ ambientCoordinateFace (prefixRooms 4 3)` with `lineMap y w (1/2) ∈ [b₂,b₃]` while `y ∉ [b₂,b₃]`. So any successful proof of the optimizer contradiction must use genuinely extra information from the minimal-face geometry or the manuscript's genericity sentence, not just upper-prefix support plus the affine line-map decomposition.
- Section 5, exact global support theorem: the development now also packages that same boundary-face language over the whole real start component as `Section5BoundaryFaceGenericity`. Its lower field is exactly the manuscript-shaped local datum `Section5MinimalSliceLowerBoundaryFaceData`, and its upper fields are the already isolated continuation-uniqueness / endpoint conditions. This support theorem now implies `Section5PerturbationGenericity` and hence the target-facet conclusion directly, so the unresolved work is precisely the geometric proof of its lower field.
- Section 5, cleaner branch target after the checked counterexample: the development now also has the stronger canonical lower-entry package `Section5CanonicalLowerEntryGenericity`. Its local field `Section5CanonicalLowerEntryData u f` records exactly the two geometric facts that the manuscript's perturbation sentence seems to promise on one non-start cell `u`: the canonical lower-prefix face already has `u.level` vertices, and the slice `section5CellSlice u f` actually meets that lower prefix face. Lean now proves immediately that this stronger package converts to `Section5EntryFaceGenericity`, then to `Section5PerturbationGenericity`, `Section5BoundarySegmentGenericity`, and the target-facet theorem. So the current main line is no longer to salvage the optimizer contradiction from the bare affine API; it is to prove this canonical lower-entry package from the manuscript's genuine local geometry.
- Section 6, first generalization theorem: when `y` is assumed not to lie in the convex hull of any `n` lattice points, the simplex `tau` containing `x` must actually be a facet. Otherwise `lambda(tau)` would lie in the convex hull of at most `n` lattice points, contradicting `lambda(x) = y`.
- Section 6, second generalization theorem: the weight choice `alpha_j = (k_j + 1/m - 1)/(n+1)` is valid because `k_j >= 1` gives `alpha_j > 0` and `sum_j alpha_j = 1` follows from `sum_j k_j = n + m`. The final counting step uses integrality: the number of positive `beta_ij` is an integer strictly greater than `k_j - 1`, hence at least `k_j`.

## Open Questions
- None at the end of the paper-check pass.
